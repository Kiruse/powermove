[package]
name = "powermove"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bevy = { version = "0.13.2", features = ["dynamic_linking"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
